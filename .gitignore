# build
dist/
.astro/

# deps
node_modules/

# env — ne jamais commiter les clés
.env
.env.production
.env.local
.env.*.local

# API AdonisJS (dossier api/) — dépendances, build et secrets
# `api/` a son propre package.json et son propre node_modules.
api/node_modules/
api/build/
api/tmp/
api/.env
api/.env.local
api/.env.*.local

# Serveur MCP du blog (dossier mcp/) — spec §7. Propre package.json/node_modules,
# déjà couvert par les règles génériques `dist/` et `node_modules/` ci-dessus ;
# explicité ici pour rester robuste si ces règles génériques sont resserrées.
mcp/node_modules/
mcp/dist/

# divers
.DS_Store
*.log
.claude/settings.local.json
